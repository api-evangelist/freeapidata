# FreeAPIData

A free, curated directory of public APIs — an editorial catalog of third-party APIs across 17 categories. FreeAPIData is itself an API-discovery directory, not an API provider; it exposes machine-readable discovery surfaces (llms.txt, apis.json, sitemap) but no consumable data API.

- **Provider:** https://freeapidata.com
- **Documentation:** https://freeapidata.com/blog
- **Base URL:** —
- **Tags:** api directory, api discovery, developer tools
- **Public API:** no
- **Agent-native (MCP / llms.txt / skills):** yes
- **Confidence:** 90/100

## APIs

- **FreeAPIData Directory** — Agent-readable API-discovery directory. Offers an llms.txt machine-readable invitation and editorial blog guidance, but no callable REST/GraphQL API. (`https://freeapidata.com`)

> FreeAPIData exposes an llms.txt (confirmed reachable and substantive) and references an apis.json self-description document (specificationVersion 0.21), though the apis.json fetch returned 'not accessible' so it is not listed as a reachable property. No OpenAPI/AsyncAPI/GraphQL/Postman contract exists. No MCP server (hosted or local) and no agent skills were found. There is no consumable REST/GraphQL data API for the directory's own data; consumers are directed to fetch public pages under robots.txt/ToS. A sitemap.xml exists but is crawl metadata, not an API contract.

_Generated by apis.io from a community submission._